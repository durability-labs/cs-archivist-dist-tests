name: Docker - KeyMaker


on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
    paths:
      - 'Tools/KeyMaker/**'
      - 'Framework/**'
      - 'ProjectPlugins/**'
      - .github/workflows/docker-keymaker.yml
  workflow_dispatch:


jobs:
  build-and-push:
    name: Build and Push
    uses: durability-labs/github-actions/.github/workflows/docker-reusable.yml@main
    with:
      docker_file: Tools/KeyMaker/docker/Dockerfile
      dockerhub_repo: durabilitylabs/archivist-keymaker
      tag_latest: ${{ github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/') }}
    secrets: inherit
