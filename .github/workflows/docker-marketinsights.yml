name: Docker - MarketInsights API


on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
    paths:
      - 'Tools/MarketInsights/**'
      - 'Framework/**'
      - 'ProjectPlugins/**'
      - .github/workflows/docker-marketinsights.yml
  workflow_dispatch:


jobs:
  build-and-push:
    name: Build and Push
    uses: durability-labs/github-actions/.github/workflows/docker-reusable.yml@main
    with:
      docker_file: Tools/MarketInsights/Dockerfile
      dockerhub_repo: durabilitylabs/archivist-marketinsights
      tag_latest: ${{ github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/') }}
    secrets: inherit
