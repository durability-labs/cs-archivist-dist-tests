name: Docker - ContractDataChecker


on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
    paths:
      - 'Tools/ContractDataChecker/**'
      - 'Framework/**'
      - 'ProjectPlugins/**'
      - .github/workflows/docker-cdt.yml
  workflow_dispatch:


jobs:
  build-and-push:
    name: Build and Push
    uses: durability-labs/github-actions/.github/workflows/docker-reusable.yml@main
    with:
      docker_file: Tools/ContractDataChecker/docker/Dockerfile
      dockerhub_repo: durabilitylabs/archivist-cdt
      tag_latest: ${{ github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/') }}
    secrets: inherit
